# Repository workflow

When a task results in code changes, commit the completed, verified changes and push the commit to the tracked remote branch before reporting completion. Do not push unverified or unrelated changes.
