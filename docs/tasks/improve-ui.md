# Improve UI

1. Read `AGENTS.md`, inspect the rendered page when browser access exists, and read the relevant HTML, CSS, and JavaScript.
2. Identify the UX/layout root cause before editing. Preserve backend behaviour and working actions.
3. Improve hierarchy, spacing, responsive layout, accessibility, keyboard/focus behaviour, and loading/error/empty/confirmation states.
4. Remove or repair dead controls; do not add fake data, fake AI scores, or decorative charts.
5. Verify desktop, tablet, and mobile layouts plus actual interactions. Run relevant tests and report only verification that occurred.
